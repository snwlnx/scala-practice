package collections

object Concat extends App {
  /**
   * Реализуем метод конкатенации двух листов,
   * не используя vat и мутабельные структуры
   */

  def concat[T](xs: List[T], ys: List[T]): List[T] = ???

  //println(concat(List(1, 2, 3), List(4, 5)))

  /**
   * Реализуйте метод squareList,
   * который возвращает новый List с квадратами
   * чисел исходного листа xs
   */

  def squareList(xs: List[Int]): List[Int] = ???

  //println(squareList(List(1, 2, 3)))

}
